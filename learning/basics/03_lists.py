# TypeScript               Python

# string[]                  list[str]

# tools.length              len(tools)

# tools.push("Pytest")      tools.append("Pytest")


tools: list[str] = ["Playwright", "Selenium", "Postman"]

print(tools)
print(tools[0])
print(len(tools))
